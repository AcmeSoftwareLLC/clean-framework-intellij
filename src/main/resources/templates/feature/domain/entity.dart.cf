import 'package:clean_framework/clean_framework.dart';

class {{feature_name}}Entity extends Entity {
  const {{feature_name}}Entity({this.id = ''});

  final String id;

  @override
  List<Object> get props => [id];

  @override
  {{feature_name}}Entity copyWith({String? id}) {
    return {{feature_name}}Entity(id: id ?? this.id);
  }
}
